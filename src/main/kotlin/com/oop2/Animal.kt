package com.oop2

abstract class Animal {
    // if can have abstract functions
    // abstract functions are functions without a body / implementation
    // only abstract classes can have abstract function
    // you're o
    abstract fun walk()
    abstract fun makeSound()

}